# G2.2 HOLD live identity collision — USB T1 stopped

**Date:** 2026-08-30  
**Canonical `64325d0e55e0435abd018defb0089a9b`:** not opened for write, not mutated.  
**EasyEDA write this lane:** none.

## What happened

The mutation gate still tracks the post-ILM HOLD sheet:

| Surface | Source hash | Designated comps | U20-USB | U1-PWR1.9 |
| --- | --- | --- | --- | --- |
| Gate / `post-ilm-saved-source.json` | `3165690:5aad2e78` | 287 | present `(400, −800)` | `USB_EFUSE_ILIM` |
| Live `getDocumentSource` (CDP dump) | `2588333:16acf63b` | 245 | **absent** | OPEN |

`get_current_context` is on HOLD `55ed9ee948734a0e903f37744b51f3b8` / page `1435cb46…`. The tab is **unsaved** (`isUnSaved: 1`).

The live buffer is **not** the sheet this lane audited. It has GT-USB-7005A and the retired USB4105, but the USB2422 / F6 / KILL-B island (`U20`–`U25`, `Y3`) is gone, J1 is at `(155, −4020)` not `(150, −4120)`, R1 has moved, and the ILM repair is not in the buffer.

## Why T1 did not run

Wiring J1 on this buffer would complete a connector on a sheet that has already lost the hub island and the closed ILM repair. That is not D-049 implementation. It is a silent overwrite.

`set_document_source` is banned. This agent did **not** save the live buffer.

## Required recovery (Captain / operator)

1. **Do not save** the current HOLD tab.
2. Reload the last **saved** HOLD document from EasyEDA (discard the unsaved buffer).
3. Re-dump. Required match: source hash `3165690:5aad2e78`, `U20-USB` present, U1-PWR1.9 on `USB_EFUSE_ILIM`.
4. If the cloud save is already the smaller sheet, stop. Restoring `post-ilm-saved-source.json` is a Captain-authorised recovery, not this lane.
5. Only then begin `g22-usb-t1-j1-migration` on the gate.

Dump: `evidence/VAL-G2-2026-08-28/g22-hold-lane/anchors/pre-usb-t1-live-dump.json`  
Audit (against the post-ILM sheet): `evidence/VAL-G2-2026-08-28/G2.2-USB-HUB-J1-PRE-AUDIT.md`

---

## RECOVERED — 2026-08-30

The unsaved-buffer recovery path failed: the 245-designator sheet was later
saved (`2588333:45fd23b6`, then `2588333:60753d61`; same non-DOCHEAD body).

Captain recovery GO restored HOLD P1 from `post-ilm-saved-source.json`.

```text
G2.2_HOLD_POST_ILM_RECOVERY = PASS
```

Receipt: `G2.2-HOLD-POST-ILM-RECOVERY-RECEIPT.md`.  
Live post-save hash: `3359274:74d46eb8`. ILM PASS. USB still FAIL on the
pre-audit T1–T6 gaps. Canonical untouched.
