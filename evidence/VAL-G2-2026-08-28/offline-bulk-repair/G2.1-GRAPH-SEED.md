# G2.1 electrical-graph seed

State: **PRE_EXPORT_SEED — not the official freeze**

This graph was extracted from the saved-and-reopened review source
(`review-source-after-reopen.json`, host hash `2352834:a75b5884`) plus the
already-captured live pin bindings. It lets G2.2 layout-engine work start
without waiting for EasyEDA to become responsive.

It is **not** the official G2.1 reference source. That freeze is a fresh
export of saved project `dcd7e3cab2a24b9aa6e531d2b62e1b6f` after the editor
is healthy. Do not promote from this seed.

```text
role:              G2.1_PRE_EXPORT_SEED
official_freeze:   false
designators:       252
named nets:        159
NO_CONNECT:        95
bound pins:        105   (PARTIAL_LIVE_BINDINGS — 14 designators)
source sha256:     50193a6ae03dbbaddafed1f1601639f405517b3842707511079cee45706a61e1
graph sha256:      892bbaee80f22d7086d42faa43557be4600b34d99b765914f7caf4c2c2a7e568
```

Spot checks against the import receipt:

- `U1-PWR1` present; `U4-PWR2` absent; `U17-PWR2` present, Convert-to-PCB = no
- `U6-RTC` two units, one Device UUID, BOM yes + no (one BOM line)
- `R8-PWR2` absent
- exact net `USB_EFUSE_PG` absent; `PWR_ENTRY_PG_RT_IOMUX_TBD` present
- `U12-NFC` has 10 NO_CONNECT pin-ids (RFO2/RFI2 remain among them)

Pin-number membership is only as complete as `review-pin-bindings.json`.
NC intent is complete at designator + symbol-pin-id resolution. Full
pin-inventory coverage waits for the official `.epro` export, which still
carries the library symbols the host source dump omits.
