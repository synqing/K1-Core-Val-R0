# G2.2 — professional schematic reconstruction

```text
STATUS = TERMINATED_BY_D-052
DATE   = 2026-08-30
```

**This programme is dead.** It is not a write licence, not a `JLC-SCH-READY`
path, and not the identity of the new board. Keep the document as evidence of
what was attempted. Draw on `K1-Core-VAL-R0-GREENFIELD` from
`architecture/GREENFIELD-BUILD-SPEC.md`.

Historical context below is frozen. Do not execute the numbered freeze /
import / equivalence steps.

---

Authority: D-048, **AMENDED_BY_D-052**. Companion ADR:
`architecture/ADR-048-g21-electrical-reference.md`. Termination ADR:
`architecture/ADR-052-greenfield-termination.md`.

## Job (historical — do not execute)

Rebuild the single electrical sheet as a proper engineered drawing while proving
**zero intentional electrical change** from the frozen hub electrical graph
(DEC-USB-HUB / D-049). The historical dual-USB G2.1 digest is not the freeze
source after H GREEN.

```text
K1-Core-Val-R0-G2.2-READABLE-CANDIDATE
```

The G2.1 project `dcd7e3cab2a24b9aa6e531d2b62e1b6f` is the electrical reference / EasyEDA
normalisation oracle. It is not the thing we beautify.

## What G2.1 is, and is not

| Claim | Status |
| --- | --- |
| EasyEDA-normalised electrical reference | YES — current reference graph |
| Proven manufacturing-clean graph | NO — ERC item text still OPEN |
| Product canonical | NO — live `64325d0e…` stays the product project |
| JLCPCB handoff | NO — `JLC-SCH-READY` attaches to GREENFIELD (D-052), not G2.2 |
| Drawing-geometry authority | NO |
| PCB layout source | NO |

```text
JLC-SCH-READY = electrically equivalent + professionally readable + EasyEDA-stable
JLC-LAYOUT-READY = JLC-SCH-READY + layout-relevant IOMUX + footprints + DXF/mechanics + pad count + JLC source package
```

`JLC-SCH-READY` unblocks RFQ/package completion and schematic handoff preparation.
It does **not** unblock paid JLCPCB placement/routing. That requires `JLC-LAYOUT-READY`.

Item-level ERC disposition on the **hub** disposable
(`41c8e6523576456582ea35958b3684ed`) is required before that electrical digest
may be frozen. Missing close-up screenshots do not gate the renderer.
Unclassified ERC fatals do. The historical `dcd7e3ca…` 9/19 host-bridge is
oracle history, not the hub freeze bar.

## Official freeze versus tonight’s seed

When EasyEDA is healthy, and without Force Quit:

1. Capture and classify every ERC item from saved hub project `41c8e652…`.
2. Repair real defects on that canvas (T25+), re-ERC, until unexplained
   fatals = 0 and known real defects = 0.
3. Export the **saved** hub project as a fresh normalised `.epro`.
4. Hash and freeze that export as the official hub electrical digest.
5. Rebuild G2.2 offline from that freeze (`epro_schematic_renderer.py`).
6. Import G2.2 into a **new** disposable EasyEDA project
   (`K1-Core-Val-R0-G2.2-READABLE-CANDIDATE`).
7. Prove exact electrical equivalence to the hub freeze.
8. Save / close / reopen / visual / ERC / BOM.
9. Only then ask Captain for `JLC-SCH-READY`.

The unpublished dual-USB digest under
`evidence/VAL-G2-2026-08-28/schematic-presentation/` is **not** the hub identity.
See `ORACLE-DIGEST.md` for the historical dcd7e3ca seed only.

If later ERC classification finds a real electrical defect, fix it **once** in the
hub source graph and regenerate G2.2. Do not hand-edit hundreds of wires.

## Transformer split

| Transformer | May change | Must not change |
| --- | --- | --- |
| G2.1 `epro_bulk_repair.py` | Electrical semantics (already closed for this archive) | — |
| G2.2 `harness/epro_schematic_relayout.py` | Geometry and annotation only | The electrical graph |

Allowed in G2.2:

- component X/Y;
- component rotation / orientation;
- wire segment geometry;
- junction positions;
- label positions and label usage;
- text notes;
- grouping graphics;
- sheet dimensions;
- title block / annotations.

Forbidden in G2.2:

- adding or removing electrical components;
- changing designators;
- changing Device UUID;
- changing symbol identity;
- changing MPN / supplier attributes;
- changing BOM or PCB-population state;
- changing footprints;
- adding or removing NC state;
- changing pin → net membership;
- changing which pins belong to each electrical net.

## Equivalence — exact, not similar

```text
DESIGNATORS             IDENTICAL
DEVICE UUIDS            IDENTICAL
MPNs                    IDENTICAL
FOOTPRINTS              IDENTICAL
BOM STATES              IDENTICAL
PCB-CONVERSION STATES   IDENTICAL
PIN INVENTORY           IDENTICAL   (once the official freeze has full coverage)
PIN → NET MEMBERSHIP    IDENTICAL
NC INTENT               IDENTICAL
ELECTRICAL NET GRAPH    IDENTICAL
```

Allowed to differ:

```text
SYMBOL POSITION
SYMBOL ORIENTATION
WIRE GEOMETRY
LABEL GEOMETRY
JUNCTION GEOMETRY
ANNOTATION
DRAWING GROUPS
SHEET SIZE
```

If one pin changes electrical membership, G2.2 fails.

Harness:

- `harness/extract_electrical_graph.py`
- `harness/check_electrical_equivalence.py`
- `harness/check_g22_pwr1_ilm.py` — fail-closed U1-PWR1.9 ILM / `USB_EFUSE_ILIM` /
  D+ continuity / R1 1.24 kΩ (ignores stale `partId`). A graph with
  `bound_pin_count=0` cannot pass this gate by geometric coincidence.
- `harness/epro_electrical_oracle.py --role G2.2_READABLE` (or `--official-freeze`)
  consumes that ILM check. ILM failure refuses the oracle, so G2.2 promotion
  and `JLC-SCH-READY` cannot close from this candidate.

HOLD `55ed9ee9…` transaction `g22-pwr1-ilm-repair-2026-08-30` restored
`U1-PWR1.9` onto `USB_EFUSE_ILIM`. Canonical `64325d0e…` was not written. The
`G2.2-HOLD-REOPEN.source.txt` dump remains a **pre-repair witness** and must
still fail this gate. This ILM blocker is cleared on the live HOLD; other G2.2
construction gaps still forbid `JLC-SCH-READY`.

## Visual grammar

Stop treating the ten boxes as sealed islands. They may survive as **subtle visual
regions**. They must not dictate connectivity.

Arrange the sheet around electrical flow:

```text
 USB / POWER ENTRY
        |
        v
 protection -> current sense -> 5V_SYS ----------------+
        |                                              |
        +-- buck -> 3V3                                |
        +-- mic rail                                   |
        +-- NFC rail                                   |
        +-- LED branch protection                      |
                                                       |
                       +-------------------------------+
                       v
                +--------------+
 AUDIO / PDM -> |    RT1062    | -> LED drive
 boot / debug ->|              |
                +------+-------+
                       |
                     K1BR
                       |
                       v
                +--------------+
                |   ESP32-S3   |
                | radio/service|
                +--+--------+--+
                   |        |
     hub DN2 (no J7)   NFC / control

 motion / ownership / validation sit beside their owners
```

Local circuits become visible wiring, not distant same-named labels.

Power (from `architecture/POWER-ARCHITECTURE.md`): USB-C → CC / VBUS → TVS → U1 eFuse →
shunt / INA226 Kelvin → `5V_SYS` → TPS62913 / mic rail / NFC rail / TPS2561 LED
branches. Disconnected `5V_SYS` labels are not a power tree.

Audio: RT1062 ↔ series / options ↔ ADC6120 ↔ analogue AUX + PDM input / clock (D-051),
with the microphone-lane PDM alternatives drawn as forks and the XOR rule beside them.
A PDM-only redraw is not electrically complete against the restored contract.

NFC: S3 control → ST25R3916B → RF network → antenna connector; matching parts tight
around the ST device; `TUNE_TBD` stated.

Motion: visible common I²C, ownership legs, interrupt route, power / decoupling.

K1BR: compact visible bus between RT and S3.

USB (D-049 `RATIFIED`): one Type-C → connector ESD → USB2422 upstream; DN1 to RT1062
OTG1; DN2 to ESP32-S3 GPIO20/19. Not two receptacles. Not two labels separated by
a blank page. The present G2.2 fixture still shows the historical dual-USB G2.1
graph; reconstructing once against the hub digest is Phase L and has not started.

The RT1062 is already multipart. Keep the same `U6` BOM device. Space units by
function (power/DCDC, audio/SAI/PDM, K1BR/control, LED, boot/reset/debug,
GPIO/options) so support circuitry wires locally. Decoupling sits beside the rail it
serves.

## What this programme does not do

- Does not write the live product project.
- Does not write `dcd7e3ca…` except later read-only export / ERC / optional zooms.
- Does not freeze the dual-USB graph as the D-049 identity.
- Does not Force Quit EasyEDA.
- Does not create a second electrical sheet.
- Does not place, convert-to-PCB, or invent a CPL.
- Does not weaken ERC or DRC.
- Does not start another MCP clicking campaign to “pretty” the reference sheet.
